# Model weights — downloaded, not shipped (524MB)
models/*.onnx
models/*.bin

# Per-machine Python environment — rebuild with uv, never transfer
.venv/

# Runtime
logs/
*.wav
__pycache__/
*.pyc

# Never commit the shared secret
token

# Tauri app build artifacts
app/node_modules/
app/dist/
app/src-tauri/target/
